Question 1: Mega Data Type

A custom CPU that has:
- Maximum addressable width = 2 bytes
- 1 byte = 8 bits
So:
2 bytes = 2 × 8 = 16 bits
Therefore, largest data type (mega) = 16 bits

Question 2: C++ Namespaces

A C++ program was written using two namespaces:
- One namespace returns int(int_namespace)
- One namespace returns long(long_namespace)

Both namespaces contain a function named "sum" that adds two numbers.

The functions are called in the main function and their results are displayed.

Question 3:
a) 4294967295
b) unsigned int stores only non-negative values
Range: 0 to 2^32 - 1 = 4294967295
when:
x = 0 - 1
The result cannot be negative
It wraps around to the maximum value
